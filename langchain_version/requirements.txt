# ──── Web 框架 ────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ──── LLM 应用框架（LangChain 版）────
# 已迁移至 langchain 1.x（create_agent 中间件体系），0.3.x 的
# AgentExecutor/create_tool_calling_agent 已移除，勿降级回 0.3
# 注：langchain-community 无 1.x 版本（能力移入 langchain-classic），项目未直接使用故不安装
langchain>=1.3.0,<2.0.0
langchain-openai>=1.4.0
langchain-text-splitters>=1.0.0
langgraph>=1.0.0,<2.0.0

# ──── 向量数据库 ────
chromadb>=0.5.0

# ──── Embedding 模型 ────
sentence-transformers>=3.0.0

# ──── PDF / Word 处理 ────
pdf2docx>=0.5.8
python-docx>=1.1.0
docx2pdf>=0.1.8

# ──── 爬虫相关 ────
DrissionPage>=4.0.0
playwright>=1.40.0
httpx>=0.25.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ──── 定时任务 ────
APScheduler>=3.10.0

# ──── 工具 ────
python-dotenv>=1.0.0
