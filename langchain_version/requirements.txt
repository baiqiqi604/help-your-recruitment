# ──── Web 框架 ────
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# ──── LLM 应用框架（LangChain 版）────
# 已迁移至 langchain 1.x（Tool-calling Agent + LCEL），0.3.x 的
# AgentExecutor/create_tool_calling_agent 已移除，勿降级回 0.3
# 注：langchain-community 无 1.x 版本（能力移入 langchain-classic），项目未直接使用故不安装
langchain==1.3.15
langchain-openai==1.4.3
langchain-text-splitters==1.1.2

# ──── 图编排/会话记忆（LangGraph 1.x）────
langgraph==1.2.11
langgraph-checkpoint==4.2.0

# ──── 向量数据库 ────
chromadb==1.5.9

# ──── Embedding 模型 ────
sentence-transformers==5.7.0

# ──── PDF / Word 处理 ────
pdf2docx==0.5.13
python-docx==1.2.0
# 简历照片插入依赖 pillow（python-docx 的 add_picture 需要它确定图片尺寸并完成缩放）
pillow==12.3.0
# docx2pdf 未在本地环境验证过（需 MS Word），保留下限不锁定
docx2pdf>=0.1.8

# ──── 爬虫相关 ────
# DrissionPage 未在本地环境验证过，保留下限不锁定
DrissionPage>=4.0.0
# playwright 仅作为 jd_crawler 扩展位预留，未在本地验证，保留下限不锁定
playwright>=1.40.0
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.1

# ──── 定时任务 ────
APScheduler==3.11.3

# ──── 工具 ────
python-dotenv==1.2.2
