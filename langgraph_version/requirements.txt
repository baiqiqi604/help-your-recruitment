# ──── Web 框架 ────
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# ──── LLM 应用框架（LangChain 基础库）────
# 已迁移至 langchain 1.x + langgraph 1.x（create_agent 替代 create_react_agent）
# 注：langchain-community 无 1.x 版本（能力移入 langchain-classic），项目未直接使用故不安装
langchain==1.3.15
langchain-openai==1.4.3
langchain-text-splitters==1.1.2

# ──── 图编排框架（LangGraph 版）────
langgraph==1.2.11
langgraph-checkpoint==4.2.0

# ──── 向量数据库 ────
chromadb==1.5.9

# ──── Embedding 模型 ────
sentence-transformers==5.7.0

# ──── PDF / Word 处理 ────
pdf2docx==0.5.13
python-docx==1.2.0
# docx2pdf 未在本地环境验证过（需 MS Word），保留下限不锁定
docx2pdf>=0.1.8

# ──── 爬虫相关（面试经验抓取，DrissionPage 浏览器渲染）────
# DrissionPage 未在本地环境验证过，保留下限不锁定
DrissionPage>=4.0.0
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.1

# ──── 工具 ────
python-dotenv==1.2.2
