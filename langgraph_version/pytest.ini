[pytest]
testpaths = tests
addopts = -q --tb=short
filterwarnings =
    # 仅忽略已知无害的第三方弃用噪音（测试桩 DeterministicEmbedding 缺少
    # chromadb 1.5.x 的 is_legacy 属性所致）；其余弃用警告保持可见以便跟踪升级
    ignore:legacy embedding function config:DeprecationWarning
