# 简历优化 Agent 双框架版本 — 今日工作总结

- **日期**：2026-08-05
- **项目**：D:/项目/aiagent（简历优化 Agent）
- **目标**：在现有 `resume_optimizer/`（LangChain 0.3 线性流水线）之外，新建 **LangChain 版**与 **LangGraph 版**两个独立项目，各写各的文档，并把两版跑起来验证效果

---

## 一、今日工作总览

| 阶段 | 内容 | 结果 |
|------|------|------|
| 1. 需求确认 | 通过提问确认目录命名、功能范围、交互方式、LLM 接入、文档组织 | ✅ 全部确认 |
| 2. 搭建 LangChain 版 | `langchain_version/`：核心流水线 + Agent + Web + 文档 | ✅ 完成 |
| 3. 搭建 LangGraph 版 | `langgraph_version/`：StateGraph 工作流 + ReAct Agent + Web + 文档 | ✅ 完成 |
| 4. 环境准备 | 创建 `.venv`，安装 langchain 0.3 栈 + langgraph 0.2 栈 | ✅ 完成 |
| 5. 流水线验证 | 两版 `optimize` 命令（mock 模式）跑通 | ✅ 通过 |
| 6. Web 验证 | 两版 5 个接口全部 200 | ✅ 通过 |

**最终目录结构**（`D:/项目/aiagent/`）：

```
aiagent/
├── README.md                      ← 根目录总览（双版本对比 + 快速开始）
├── start_web.py                   ← Web 服务分离启动脚本
├── .venv/                         ← 独立 Python 虚拟环境
├── resume_optimizer/              ← 历史初版（保留参考）
├── langchain_version/             ← LangChain 版（17 个文件）
│   ├── config.py / llm_client.py / jd_analyzer.py / content_optimizer.py
│   ├── resume_reader.py / resume_writer.py / jd_knowledge_base.py
│   ├── jd_crawler.py / scheduler.py
│   ├── agent.py                   ← Tool-calling Agent + AgentExecutor + 会话记忆
│   ├── web_app.py / main.py / templates/index.html
│   └── docs/PRD.md、docs/技术开发文档.md
└── langgraph_version/             ← LangGraph 版（18 个文件，多 graph.py）
    ├── graph.py                   ← StateGraph：load→analyze→optimize→review→write
    ├── agent.py                   ← ReAct Agent + MemorySaver + thread_id 记忆
    ├── 共享模块 / web_app.py / main.py / templates/index.html
    └── docs/PRD.md、docs/技术开发文档.md
```

---

## 二、需求确认（开工前提问）

开工前向用户确认了 6 个关键决策点：

1. **目录命名**：`D:/项目/aiagent/langchain_version/` 与 `langgraph_version/`，与旧版平级、各自独立完整
2. **功能范围**：核心流程 + JD 知识库检索 + JD 爬虫 + 定时任务 + PDF/Word 读写 + **对话式求职建议**
3. **交互方式**：**Web 界面**（FastAPI + 轻量前端）
4. **LLM 接入**：**可配置多 Provider**（DeepSeek / OpenAI / 通义 / 智谱，OpenAI 兼容接口）
5. **Web 技术栈**：FastAPI + 轻量前端
6. **文档组织**：各版本独立文档（每个版本目录内 PRD + 技术开发文档），根目录只留总览

---

## 三、双版本搭建

### 3.1 共享核心模块（由主 Agent 手写，两版逐字一致）

| 模块 | 职责 |
|------|------|
| `config.py` | 多 Provider 配置（`LLM_PROVIDERS` 字典 + 环境变量），路径 / 向量库 / 爬虫 / 调度 / Web 配置 |
| `llm_client.py` | OpenAI 兼容多 Provider 客户端；`chat()` / `chat_json()` / `chat_json_array()` + JSON 容错解析 |
| `jd_analyzer.py` | 调 LLM 从 JD 提取技能 / 职责 / 经验 / 关键词（结构化 JSON） |
| `content_optimizer.py` | 按「定制简历九大原则」优化简历 + 构建简历-JD 匹配关系表 |

### 3.2 版本差异（Agent 编排层）

| 维度 | LangChain 版 | LangGraph 版 |
|------|--------------|--------------|
| Agent 构建 | `create_tool_calling_agent` + `AgentExecutor` | `create_react_agent` + `MemorySaver` |
| 工作流 | 线性函数调用 + 工具路由 | `StateGraph`：load→analyze→optimize→**review**→write，条件边 + 失败重试（≤3 次） |
| 会话记忆 | `ConversationBufferMemory`（session_id 缓存） | `MemorySaver` + thread_id（checkpoint 机制） |
| 新增依赖 | 无 | `langgraph`、`langgraph-checkpoint` |

### 3.3 文档重写

- 每个版本目录内：`docs/PRD.md`（产品需求文档）+ `docs/技术开发文档.md`（架构 / 模块 / Agent 设计 / API / 部署）
- 根目录：`README.md` 总览（双版本对比表 + 快速开始 + 目录结构）

---

## 四、遇到的问题与解决方案

> 按"问题 → 现象 → 原因 → 解决"记录，按时间顺序排列。编号与代码提交无关，仅供索引。

### 问题 1：`langchain-agents` 包不存在，pip 安装直接失败

- **现象**：`pip install -r requirements.txt` 报错：
  ```
  ERROR: Could not find a version that satisfies the requirement langchain-agents
  ```
- **原因**：`requirements.txt` 里写了 `langchain-agents>=0.3.0`，但 PyPI 上**没有这个独立包**（`AgentExecutor` / `create_tool_calling_agent` 在 langchain 0.3 主包内，不需要单独安装）。
- **解决**：从两版 `requirements.txt` 中删除 `langchain-agents` 一行；`AgentExecutor` 直接从 `langchain.agents` 导入（代码本就是这种写法，依赖清单写错了）。

### 问题 2：pip 默认源超时，依赖装不上

- **现象**：`pip install` 连续两次 300 秒超时，`.venv` 里始终只有 pip / setuptools，langchain 一直没装上。
- **原因**：默认 PyPI 源（pypi.org）网络慢；且一条命令装 20+ 个包（含 langchain 全家桶）耗时过长。
- **解决**：
  - 换**清华镜像源**：`--index-url https://pypi.tuna.tsinghua.edu.cn/simple --timeout 60`（一次成功）；
  - **分小批安装**：先装 langchain 0.3 核心栈，再装 Web/工具依赖，避免单条命令超时。

### 问题 3：全局环境装了 langchain 1.x，与代码 0.3 API 不兼容

- **现象**：`import agent` 报错：
  ```
  ImportError: cannot import name 'AgentExecutor' from 'langchain.agents'
  ```
- **原因**：全局 Python 环境里 pip 默认装到了 **langchain 1.3.14**，而 1.x **移除了 `AgentExecutor` / `create_tool_calling_agent`**（代码按 langchain 0.3 API 编写）。
- **解决**：
  - 创建独立虚拟环境 `.venv` 隔离全局环境；
  - 安装 **langchain 0.3.29 栈**（`langchain>=0.3.0,<0.4.0`，含 langchain-community 0.3.31 / langchain-openai 0.3.35 / langchain-text-splitters 0.3.11）；
  - 同步修正两版 `requirements.txt`，加版本上界和注释，防止日后误装 1.x；
  - 两版 22 个模块 import 全部通过验证。

### 问题 4：langgraph_version 缺 langgraph 依赖

- **现象**：跑 langgraph 版流水线报：
  ```
  ImportError: 缺少依赖 langgraph，请执行: pip install langgraph langgraph-checkpoint
  ```
- **原因**：`graph.py` 在 `build_graph()` 里做了懒加载（try/except），而 `.venv` 里只装了 langchain 栈，没装 langgraph。
- **解决**：在 `.venv` 内安装 **langgraph 0.2.76 + langgraph-checkpoint 2.1.2**。注意要配 `langgraph>=0.2.0,<0.3.0`——langgraph 1.x 需要 langchain-core 1.x，与 langchain 0.3 不兼容，不能装最新版。

### 问题 5：curl 发中文 JSON 报 "There was an error parsing the body"

- **现象**：`curl -d '{"resume_text":"姓名：张三…"}'` 调 `/api/optimize` 返回 422：
  ```
  {"detail":"There was an error parsing the body"}
  ```
- **原因**：Git Bash 下 `curl -d` 内联中文 JSON 的**编码/转义处理**不可靠（单引号内 `\n` 与中文混排）。
- **解决**：
  - 改为 `curl --data-binary @req.json`（文件方式发送）规避 shell 转义；
  - 最终验证改用 Python `httpx` 直接 POST（`json=` 参数自动处理编码），彻底避开终端编码问题。

### 问题 6：后台启动 uvicorn 被 bash 会话连带终止

- **现象**：`nohup … &` 启动 uvicorn 后日志显示 "Uvicorn running on 8000"，但下一个命令 curl 就 **connection refused**（exit 7），端口无监听。
- **原因**：bash 工具会话结束/超时时，**连同子进程一起清理**，后台 uvicorn 被杀。
- **解决**（两种方案）：
  1. **单次命令内完成"启动→验证→清理"**：一个 bash 调用里 `uvicorn … &` → `sleep 9` → curl/httpx 验证 → `kill $PID`，进程生命周期不跨会话；
  2. 提供 `start_web.py`（Python `subprocess.Popen` + `DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP` 标志 + 重定向日志 + `close_fds=True`）真正脱离会话，可让服务常驻。

### 问题 7：`/api/jobs/search`、`/api/jobs/premium` 返回 500

- **现象**：两个岗位查询接口 `Internal Server Error`。
- **原因**：环境未安装 **chromadb**（重型依赖，未纳入最小运行集），`init_kb()` / `_get_collection()` 抛异常。LangChain 版 `get_premium_jobs` 里 `_premium_collection.get()` 在 try 块外，异常直接冒泡成 500。
- **解决**：给 `jd_knowledge_base.py` 加**优雅降级**：
  - 新增模块级标志 `_kb_available = True`；
  - `init_kb()` 捕获 `ImportError`（缺 chromadb）与 embedding 模型加载失败，记 warning 后置 `_kb_available = False` 并返回，**不再抛异常**；
  - 各查询函数（`search_jds` / `get_job_by_id` / `get_premium_jobs` / `count_jobs`）检查标志，不可用时返回空列表/None；
  - LangGraph 版（结构不同）对 `_get_collection()` 调用逐个包 try/except，同样降级返回空。
  - 效果：缺依赖时接口返回 `{"count":0,"jobs":[]}` 而不是 500，装上 chromadb 后自动恢复完整功能。

### 问题 8：mock 模式下 `/api/chat` 仍会因缺 API Key 报错

- **现象**：无 API Key 时，对话接口底层 `get_llm()` 抛 `ValueError: 未配置 … API Key`。
- **原因**：`agent.py` 的 `chat_with_agent()` 会走 `get_agent()` → `get_llm()` 构建真实模型，mock 模式只覆盖了 `chat()` / `chat_json()`，没覆盖 Agent 构建路径。
- **解决**：在两版 `agent.py` 的 `chat_with_agent()` 入口加分支：
  ```python
  if llm_client.mock_enabled():
      logger.info("[MOCK] 对话模式：返回模拟回复")
      return llm_client.chat(user_input)
  ```
  mock 模式下对话直接返回模拟回复，不再构建真实 Agent。

### 问题 9：docx 转 PDF 失败（环境限制，非代码缺陷）

- **现象**：流水线结尾 WARNING：
  ```
  docx 转 PDF 失败（可能需要安装 MS Word）: No module named 'docx2pdf'
  ```
- **原因**：`docx2pdf` 依赖本机安装 **Microsoft Word**（COM 接口），当前环境未满足。
- **解决**：属**可选能力**，失败只记 warning 不中断主流程；DOCX 与匹配表 JSON 正常产出。后续装好 MS Word 或改走 LibreOffice 转换即可。

### 问题 10：mock 支持是临时加的，需与真实调用共存

- **现象**：为演示流水线需要"无 Key 也能跑"，但 mock 不能污染正常调用路径。
- **原因**：临时需求（演示效果），需要可开关。
- **解决**：`llm_client.py` 增加 `mock_enabled()`（读环境变量 `MOCK_LLM=1`）与 `_mock_chat()`（按 prompt 特征返回 JD 分析 / 匹配表 / 优化简历 / 对话四类模拟响应），仅在开关打开时生效；`.env.example` 注释说明。配好真实 Key 后不设 `MOCK_LLM` 即为真实调用。

---

## 五、运行验证结果（mock 模式）

> 因环境无真实 API Key，验证使用 `MOCK_LLM=1` 演示模式；`llm_client.chat()` 返回模拟响应，流水线与 Web 接口完整走通。

### 5.1 环境与依赖（最终版本）

| 组件 | 版本 |
|------|------|
| Python | 3.11.8（虚拟环境 `D:/项目/aiagent/.venv`） |
| langchain | 0.3.29（0.3 栈，兼容代码 API） |
| langchain-community / langchain-openai / langchain-text-splitters | 0.3.31 / 0.3.35 / 0.3.11 |
| langgraph / langgraph-checkpoint | 0.2.76 / 2.1.2 |
| fastapi / uvicorn / httpx / python-docx | 0.141.1 / 0.52.1 / 0.28.1 / 1.2.0 |

### 5.2 模块导入验证

- langchain_version：11 个模块全部 import 通过
- langgraph_version：12 个模块（含 `graph.py`）全部 import 通过

### 5.3 核心流水线（CLI）

| 版本 | 命令 | 结果 |
|------|------|------|
| LangChain 版 | `main.py optimize --resume input/sample_resume.txt --jd input/sample_jd.txt` | ✅ JD 分析（核心技能 4 项）→ 简历优化（645 字）→ 匹配表（3 条）→ 输出 `output/sample_resume_optimized.docx` + `sample_resume_matching_table.json` |
| LangGraph 版 | 同参数 | ✅ StateGraph 五节点流程（load→analyze→optimize→review→write）跑通 → 输出 `output/optimized_*.txt` + 匹配表 |

### 5.4 Web 接口验证（FastAPI）

| 接口 | LangChain 版 (:8000) | LangGraph 版 (:8001) |
|------|:---:|:---:|
| `GET /`（前端页面） | 200 | 200 |
| `POST /api/chat`（对话） | 200 | 200 |
| `POST /api/optimize`（优化） | 200（645 字 + 3 条匹配表） | 200（同） |
| `GET /api/jobs/search`（岗位检索） | 200（`{"count":0,"jobs":[]}`） | 200（空） |
| `GET /api/jobs/premium`（优质岗位） | 200（空） | 200（空） |

> 岗位检索返回空是**预期行为**：chromadb 未安装，知识库优雅降级为空；安装依赖并入库数据后即可正常检索。

---

## 六、遗留事项与后续建议

1. **配置真实 API Key 看真效果**：复制 `.env.example` 为 `.env`，填 `DEEPSEEK_API_KEY`（或其他 provider 的 Key），不设 `MOCK_LLM` 重跑，即走真实模型。
2. **知识库启用**：`pip install chromadb sentence-transformers`（较重，会拉 torch），首次启动会下载 BGE 中文 Embedding 模型，之后岗位入库/检索自动生效。
3. **PDF 输出**：docx→pdf 需要本机安装 MS Word（`docx2pdf` 依赖 COM）；或改用 LibreOffice 转换方案。
4. **爬虫为骨架实现**：`jd_crawler.py` 提供 httpx+bs4 四平台框架，真实站点反爬策略需按平台维护；`playwright`/`DrissionPage` 依赖未装，按需启用。
5. **版本锁定**：两版 `requirements.txt` 已加版本上界（langchain <0.4、langgraph <0.3），后续升级需先确认 API 兼容性。
6. **代码质量**：`test_pipeline.py`（旧版）未移植到两版，可按需补单元测试。

---

*本文档由 AtomCode 于 2026-08-05 开发过程中记录整理。*


