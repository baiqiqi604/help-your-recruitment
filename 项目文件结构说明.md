# 项目文件结构说明

项目根目录：`D:/项目/aiagent`

> **标注说明**：⭐ = 核心文件（主开发版的关键模块，新功能开发/排障优先从这里入手）

## 一、项目根目录

| 路径 | 类型 | 作用 |
|---|---|---|
| `README.md` | 文件 | 项目总说明（架构、启动方式、RAG 检索策略、功能清单） |
| `CHANGELOG.md` | 文件 | 变更日志（按 git 提交记录，每次提交后同步更新） |
| `start_web.py` | 文件 | 独立启动 uvicorn Web 服务脚本（脱离 bash 会话） |
| `.github/workflows/ci.yml` | 文件 | GitHub Actions CI：Python 3.10/3.11 矩阵跑 pytest 测试（MOCK_LLM 模式，覆盖率 ≥60%） |
| `models/` | 目录 | 本地模型目录（`bge-reranker-v2-m3` 精排模型，存在即优先加载，避免联网下载） |
| `docs/` | 目录 | 文档：`LangChain_1_3_15迁移评估报告.md` + `_archive/` 历史归档 |
| `docs/_archive/` | 目录 | 历史文档归档（初版 PRD、旧版技术文档、开发日志，已移至此处） |
| `.git/` | 目录 | Git 版本库 |
| `.gitignore` | 文件 | Git 忽略规则（含 `models/`、`web_verify.*`） |
| `.venv/` | 目录 | 主 Python 虚拟环境（Python 3.11.8，打包/运行用） |
| `.venv_v1/` | 目录 | 备用虚拟环境（Python 3.11.8，原 `.venv-codex312` 已移除） |
| `.vscode/` | 目录 | VSCode 配置 |
| `resume_optimizer/` | 目录 | **第一代旧版**单文件体系（早期原型）🔒 已冻结，仅参考 |
| `langchain_version/` | 目录 | **第二代 LangChain 版**（Tool-calling Agent + LCEL 管道）✅ 并行开发版（与主版同步） |
| `langgraph_version/` | 目录 | **第三代 LangGraph 版**（ReAct Agent，当前主开发版）✅ 主力维护版本（与 langchain 版并行开发） |

## 二、langgraph_version（主开发版，已打包 exe）

### 核心后端模块（⭐ 为核心文件）

| 文件 | 作用 |
|---|---|
| ⭐ `web_app.py` | FastAPI Web 服务入口：Chat 对话（确定性 RAG 分流 + `/api/chat/stream` SSE 流式）、简历优化、上传/下载、题库/岗位检索接口 |
| ⭐ `agent.py` | LangGraph ReAct Agent（RAG 优先答疑，MemorySaver 多轮记忆，`astream_chat` 流式） |
| ⭐ `graph.py` | 简历优化 LangGraph 流水线（定制简历 + 匹配表） |
| ⭐ `llm_client.py` | 多 Provider LLM 客户端封装（DeepSeek/OpenAI/通义/智谱），含 stream_chat / chat_structured |
| ⭐ `config.py` | 全局配置：LLM、Embedding、ChromaDB、路径、爬虫、Web 端口、RERANK 配置（支持 exe 打包路径） |
| ⭐ `reranker.py` | bge-reranker-v2-m3 跨编码器精排：粗召回 top_k×5 后逐对打分取 top_k；懒加载 + 优雅降级 |
| `schemas.py` | 结构化输出 Pydantic 模型（JDAnalysis / MatchingRow，配合 chat_structured） |
| `retrievers.py` | 检索层 Retriever 抽象（InterviewKBRetriever / JDKBRetriever，继承 BaseRetriever） |
| `interview_knowledge_base.py` | 面试题库检索层（标题索引 → 相似度阈值过滤 → 关键词兜底 → Rerank 精排） |
| `jd_knowledge_base.py` | 岗位知识库 + BGE Embedding 封装（query 前缀优化 + 标题索引） |
| `jd_analyzer.py` | 岗位描述（JD）分析：提取技能/职责/要求 |
| `company_researcher.py` | 目标公司调研（LLM 生成公司研究） |
| `content_optimizer.py` | 简历内容优化 + 简历-JD 匹配表构建 |
| `interview_advisor.py` | 面试建议 + 面试问题生成 |
| `resume_reader.py` | 简历文件解析（PDF/DOCX/TXT → 文本） |
| `resume_writer.py` | 生成定制化简历/面试建议 Word 文档 |
| `experience_crawler.py` | 面经爬虫 + 手动素材保存 |
| `experience_processor.py` | 面经结构化处理（LLM 抽取题目/答案） |

### 数据导入脚本（一次性工具）

| 文件 | 作用 |
|---|---|
| `_ingest_ai_pm_bank.py` | 导入 AI 产品经理 1000 题题库 |
| `_ingest_ai_agent_bank.py` | 导入 AI Agent 工程师 1000 题题库 |
| `_ingest_experiences.py` | 导入经验素材 |
| `_merge_import_banks.py` | 合并多个题库导入 |
| `_rebuild_ai_pm_bank.py` | 重建 AI 产品经理题库 |
| `_replace_ai_pm_bank.py` | 用扩展版替换原题库 |

### 测试与质量（新增）

| 文件 | 作用 |
|---|---|
| `tests/` | pytest 测试套件：`conftest.py`（RERANK_ENABLED=0 保证确定性）+ 7 个用例文件（114 用例：图流程 / Web API / 知识库真实检索 / 简历 IO / LLM JSON 等） |
| `pytest.ini` | pytest 配置 |
| `requirements-dev.txt` | 测试/覆盖率依赖（pytest-cov 等） |

### 启动与验证

| 文件 | 作用 |
|---|---|
| `main.py` | 命令行入口（流程演示/调试） |
| `start_web_offline.py` | 离线启动服务（HF 离线环境变量） |
| `start_server.bat` | Windows 计划任务启动脚本（脱离会话） |
| `validate_runtime.py` | 运行环境依赖校验 |
| `requirements.txt` | Python 依赖清单 |

### 数据与资源目录

| 路径 | 作用 |
|---|---|
| `data/chroma_db/` | **向量数据库**（题库 1972 题，含 HNSW 索引 + `jd_title`/`interview_kb_title` 标题索引集合） |
| `data/chroma_db_backup_20260810_*/` | 4 个时间点备份（140344/165622/191144/211937） |
| `data/crawled_jobs/` | 爬取的岗位数据存档 |
| `data/raw/` | 原始素材（AI_Agent工程师1000题面经总结.docx、experience/） |
| `input/` | 样例输入（sample_jd.txt、sample_resume.txt） |
| `output/` | 生成的简历/面试建议 docx 输出目录 |
| ⭐ `templates/index.html` | 前端单页应用（青绿配色 + Tabler Icons） |
| `static/tabler-icons/` | 本地化图标资源（字体 + CSS） |
| `docs/` | 文档：PRD、启动指南、技术开发文档、面试笔试经验知识库设计 |
| `desktop/` | **exe 打包目录**（PyInstaller） |
| `desktop/launcher.py` | exe 启动器（复制数据 + 开浏览器 + 退出窗口） |
| `desktop/build.spec` | PyInstaller 打包配置 |
| `desktop/build_run.bat` | 一键打包脚本 |
| `desktop/dist/AI简历优化Agent/` | **成品 exe**（AI简历优化Agent.exe + `_internal/` 运行时 + `data/` + `hf_model/` 本地模型 + `input/` + `static/` + `templates/`） |
| `web_out.log` / `web_err.log` | 服务运行日志 |
| `web_run.log` | 运行日志（含启动详情） |
| `web_verify.log` / `web_verify.pid` | 启动自检日志 / 进程 PID 文件 |

## 三、langchain_version（第二代，并行开发版）

> 自 2026-08-15 起由「冻结参考版」转为**并行开发版**：改动先在 langgraph 版落地，随后同步到本版。

| 文件/目录 | 作用 |
|---|---|
| `web_app.py` | FastAPI 服务（确定性 RAG 分流 + `/api/chat/stream` SSE 流式 + 预热 + 健康检查） |
| `agent.py` | LangChain Tool-calling Agent（create_agent + MemorySaver 会话记忆，含 astream_chat） |
| `llm_client.py` | LLM 客户端封装（与 langgraph 版一致，含 stream_chat / chat_structured） |
| `config.py` | 配置（**共享 langgraph 版向量库** chroma_db，含 RERANK 配置） |
| `schemas.py` | 结构化输出 Pydantic 模型（与 langgraph 版一致） |
| `retrievers.py` | 检索层 Retriever 抽象（与 langgraph 版一致） |
| `chain.py` | **LCEL 简历优化管道**（RunnableSequence：load → analyze → research → optimize → matching → review → interview → write） |
| `reranker.py` | RAG 精排（已同步 langgraph 版：bge-reranker-v2-m3 + 优雅降级） |
| `interview_knowledge_base.py` | 题库检索（已同步标题索引 + 关键词兜底 + Rerank） |
| `jd_knowledge_base.py` | 岗位库 + BGE Embedding（query 前缀 + 标题索引已同步） |
| `jd_analyzer.py` / `jd_crawler.py` | JD 分析（结构化解析）/ 岗位爬虫（旧版爬虫） |
| `company_researcher.py` / `content_optimizer.py` / `interview_advisor.py` | 公司调研 / 简历优化（纯文本清洗）/ 面试建议 |
| `resume_reader.py` / `resume_writer.py` | 简历解析 / Word 生成 |
| `experience_crawler.py` / `experience_processor.py` | 面经爬取 / 结构化 |
| `scheduler.py` | 定时任务（langchain 版独有） |
| `_ingest_ai_agent_bank.py` / `_ingest_ai_pm_bank.py` / `_ingest_experiences.py` / `_merge_import_banks.py` / `_rebuild_ai_pm_bank.py` / `_replace_ai_pm_bank.py` | 题库/素材导入脚本（已同步 langgraph 版） |
| `main.py` / `start_web_offline.py` / `start_server.bat` / `validate_runtime.py` / `requirements.txt` | 入口（web/chat/doctor/optimize）/ 离线启动 / 计划任务启动 / 依赖校验 / 依赖 |
| `tests/` / `pytest.ini` / `requirements-dev.txt` | pytest 测试套件 / 配置 / 测试依赖（已同步） |
| `.env` / `.env.example` | 环境变量配置 / 示例 |
| `data/chroma_db/` | 向量库（软共享 langgraph 版数据） |
| `templates/index.html` | 前端（聊天打字机 + 复制按钮，标题为 LangChain） |
| `static/tabler-icons/` | 图标资源（已同步） |
| `docs/` | LangChain版升级总结.md、PRD.md、技术开发文档.md |
| `input/` / `output/` | 输入样例 / 输出文档 |
| `web_out.log` / `web_err.log` / `web_run.log` | 运行日志 |

## 四、resume_optimizer（第一代旧版，已停用）

| 内容 | 作用 |
|---|---|
| `config.py` `llm_client.py` `jd_analyzer.py` `jd_crawler.py` `jd_knowledge_base.py` `content_optimizer.py` `resume_reader.py` `resume_writer.py` `scheduler.py` `main.py` | 早期单体版：简历优化 + JD 分析 + 岗位爬虫 |
| `test_pipeline.py` | 旧版流水线测试 |
| `data/` `input/` `output/` | 旧版数据/输入/输出目录 |

## 版本关系速览

```
resume_optimizer（v1 旧版）→ langchain_version（v2 LangChain）→ langgraph_version（v3 LangGraph，主版）
```

- **langgraph_version**：主力版本（功能最新：LangGraph 图编排 + 面试题库 RAG + 标题索引/Rerank 精排 + 结构化输出 + SSE 流式 + pytest 测试体系 + CI + 桌面 exe 打包），打包产物在 `desktop/dist/AI简历优化Agent/`
- **langchain_version**：**并行开发版**（自 2026-08-15 起，改动先在 langgraph 版落地后同步到本版）；与 langgraph 版共享向量库/检索层/结构化输出/Retriever 抽象/SSE 流式，框架差异为 **LangChain（Tool-calling Agent + LCEL 管道）** vs **LangGraph（StateGraph 图编排）**，另有 `scheduler.py` 定时任务
- 两者核心差异仅在 Agent/流水线框架（LangGraph vs LangChain）与爬虫/打包能力

*生成日期：2026-08-11*（本次更新：2026-08-15）
