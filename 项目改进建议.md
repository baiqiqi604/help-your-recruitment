# 简历优化 Agent 项目改进建议

本文基于《简历优化 Agent — 技术开发文档》v1.1 进行评审，重点关注项目能否安全、稳定、可验证地落地。

## 一、总体判断

当前文档已经覆盖核心业务流程、技术选型、接口草案和开发排期，适合作为概念验证阶段的设计稿。但项目同时包含简历解析、文档排版、LLM 改写、RAG、四个平台爬虫和定时任务，首版范围偏大；数据合规、效果评估、事实一致性和工程化设计也需要进一步明确。

建议先将 MVP 聚焦为“用户上传简历 + 手动粘贴 JD + 输出逐条修改建议或结构化 Word”，验证优化效果后，再逐步加入岗位知识库、自动爬虫和复杂格式还原。

## 二、优先级最高的改进项

### 1. 缩小 MVP 范围

文档计划在较短时间内同时完成文档转换、RAG、四个平台爬虫、定时调度和 CLI，依赖较多且风险相互叠加。建议拆分为：

1. MVP：手动输入 JD、解析 DOCX/PDF、生成修改建议、人工确认后导出 DOCX。
2. 第二阶段：加入优化前后对比、事实校验和效果评分。
3. 第三阶段：加入岗位知识库及检索。
4. 第四阶段：在完成合规评估后接入允许的数据源或官方接口。

首版暂不承诺“与原 PDF 完全一致”的排版效果，避免格式还原问题阻塞核心价值验证。

### 2. 将“事实约束”设计为系统能力

仅在 Prompt 中要求“不编造经历”不够可靠。建议建立结构化事实层：

- 从原简历提取公司、职位、时间、项目、技术、数字成果等事实。
- 每条优化内容必须关联原文证据，保存来源段落 ID。
- 禁止新增原文中不存在的公司、技能、职责、时间和量化结果。
- 输出优化稿前执行规则校验与模型复核。
- 对新增、删除和改写内容生成差异报告，交由用户确认。

### 3. 补充可量化的效果评估

目前“提升匹配度”没有明确验收标准。建议至少建立以下指标：

| 指标 | 说明 |
|---|---|
| 事实一致率 | 优化内容中可被原简历支持的陈述比例 |
| JD 关键词覆盖率 | 核心关键词在优化稿中的合理覆盖比例 |
| 人工采纳率 | 用户最终接受的改写条目比例 |
| 格式保真度 | 导出前后页面数、段落、表格和样式差异 |
| 任务成功率 | 从上传到成功导出的完整流程成功比例 |
| 成本与延迟 | 单次优化的模型费用和响应时间 |

同时准备一组去标识化测试样本，覆盖应届生、社招、技术岗、非技术岗、单栏、多栏、表格和扫描件等情况。

### 4. 重新评估岗位爬虫方案

“绕过反爬”“自动登录”“代理池”和验证码处理可能带来平台协议、账号安全、个人信息及法律合规风险。建议：

- 优先使用官方 API、公开数据、用户主动提供的 JD 或获得授权的数据源。
- 为每个数据源记录许可范围、抓取频率、保留期限和删除机制。
- 不在代码或配置文件中保存账号密码、Cookie、令牌和个人信息。
- 将爬虫作为可插拔模块，与简历优化主流程解耦。
- 为数据源设置熔断和下线开关，避免单个平台变化影响主产品。

### 5. 调整文档处理策略

PDF 本质上是最终展示格式，转成 Word 后再逐段替换容易出现文本框、字体、表格、多栏布局和分页错位。建议：

- 优先支持 DOCX 输入，PDF 作为兼容输入。
- 对 PDF 分为文本型、扫描型和复杂排版型，并采用不同处理策略。
- 将内容层与展示层分离，先生成结构化简历数据，再渲染到模板。
- 对复杂 PDF 提供“修改建议模式”，而不是强制生成格式一致的文件。
- 输出 PDF 时使用跨平台渲染方案，并明确 Windows/Office 依赖。

## 三、架构与工程改进

### 1. 明确领域数据模型

建议为 `Resume`、`ResumeSection`、`Experience`、`JobDescription`、`JobRequirement`、`OptimizationSuggestion` 和 `Evidence` 定义稳定的数据结构，并通过模型校验字段。不要让各模块长期依赖自由格式字典，否则接口容易随 Prompt 输出变化而失效。

### 2. 降低对 LangChain 的耦合

LangChain 可以用于快速验证，但核心业务接口应由项目自身定义。建议把模型调用、Embedding、向量库和 Prompt 封装为适配器，使后续能够替换模型供应商或直接调用 SDK，减少框架升级造成的影响。

### 3. 验证 RAG 是否真正必要

针对“单份简历 + 单个 JD”的优化，完整上下文通常可以直接输入模型，未必需要先做向量检索。岗位知识库更适合岗位推荐、行业技能统计、相似岗位检索和历史 JD 分析。建议先通过实验比较“直接上下文”和“RAG”在质量、成本及延迟上的差异，再决定是否进入主链路。

### 4. 增加任务状态和可恢复机制

文档转换、模型调用和导出都可能失败。建议将一次优化建模为任务，记录 `pending`、`parsing`、`analyzing`、`optimizing`、`reviewing`、`exporting`、`completed`、`failed` 等状态，并支持重试、幂等和失败后从中间步骤恢复。

### 5. 完善配置与依赖管理

- 使用环境变量或密钥管理服务保存 API Key。
- 提供 `.env.example`，但不提交真实密钥。
- 锁定依赖版本并区分核心依赖、爬虫依赖和开发依赖。
- 配置模型超时、重试、并发、速率限制和费用上限。
- 明确 Python、浏览器、Office 或 LibreOffice 等运行环境要求。

### 6. 增加可观测性

为每次任务生成唯一 ID，记录各步骤耗时、错误类型、模型名称、Token 用量和导出结果。日志必须去除姓名、电话、邮箱、住址、公司敏感信息和完整简历正文，避免调试日志造成隐私泄露。

## 四、安全与隐私改进

简历包含高度敏感的个人信息，建议在开发前补充数据安全设计：

- 明确用户授权、隐私说明、数据用途和保留期限。
- 默认最短时间保存原始简历和生成文件，并支持立即删除。
- 本地文件、数据库和备份采用访问控制，必要时加密。
- 发送给第三方模型前进行告知，并尽量提供脱敏或本地模型选项。
- 不将用户简历用于训练、公开测试集或岗位知识库。
- 防范 JD 中的提示注入，将 JD 视为不可信数据而非系统指令。
- 对上传文件限制类型、大小、页数，并进行恶意文件检查。

## 五、测试与验收改进

建议建立分层测试：

- 单元测试：文本分段、结构化解析、事实校验、去重和状态转换。
- 契约测试：模型结构化输出、向量库适配器和导出接口。
- 集成测试：从输入简历和 JD 到生成 DOCX 的完整链路。
- 回归测试：固定样本上的优化结果、事实一致率和格式差异。
- 故障测试：模型超时、限流、空 JD、损坏文件、扫描 PDF 和导出失败。

模型输出存在随机性，测试不应只比较完整字符串，而应校验结构、约束、证据关联和关键业务指标。

## 六、产品体验改进

- 在生成最终文件前展示逐条建议及修改理由，允许接受、拒绝和手动编辑。
- 区分“必须修改”“建议修改”“可选润色”，避免一次性大幅改写。
- 展示关键词覆盖依据，但避免机械堆砌关键词。
- 明确提示优化不能保证通过 ATS 或获得面试。
- 支持保存多个岗位版本，并标注对应 JD、生成时间和模型版本。
- 为没有量化成果的经历提供提问引导，而不是自动编造数字。

## 七、建议的项目结构

```text
resume_optimizer/
├── src/resume_optimizer/
│   ├── domain/              # 领域模型与校验规则
│   ├── application/         # 优化任务编排
│   ├── adapters/            # LLM、向量库、文件转换适配器
│   ├── parsers/             # DOCX、PDF、OCR 解析
│   ├── exporters/           # DOCX、PDF 输出
│   ├── evaluation/          # 事实、匹配度、格式评估
│   └── interfaces/          # CLI 或 Web 接口
├── tests/
│   ├── fixtures/            # 去标识化测试样本
│   ├── unit/
│   └── integration/
├── docs/
├── data/                    # 默认不纳入版本控制
├── .env.example
├── pyproject.toml
└── README.md
```

## 八、推荐实施顺序

1. 定义结构化数据模型、事实约束和验收指标。
2. 完成 DOCX 输入、手动 JD、建议生成和人工确认闭环。
3. 增加 DOCX 导出、差异报告、日志脱敏和自动化测试。
4. 验证 PDF 解析及复杂排版降级策略。
5. 用真实样本比较直接上下文与 RAG 的收益。
6. 在完成数据合规评估后再接入岗位数据源。

按此顺序推进，可以优先验证“优化内容是否真实且有效”这一核心价值，同时把爬虫、复杂排版和基础设施风险隔离到后续阶段。
